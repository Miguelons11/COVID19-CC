version: '3' 
services: 
  api:
#   Construcción a parte para hacerla con Jenkins
#    build: .
    image: covidapi
    container_name: covidapi-container
    network_mode: "host"
    expose:
      - "4000"
  app:
#   Construcción a parte para hacerla con Jenkins
#    build: client/
    image: covidapp
    container_name: covidapp-container
    network_mode: "host"
    expose:
      - "8080"
    depends_on:
      - api
